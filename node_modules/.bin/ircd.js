#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ircd/bin/ircd.js" "$@"
  ret=$?
else 
  node  "$basedir/../ircd/bin/ircd.js" "$@"
  ret=$?
fi
exit $ret
